
let musicOn = false;//音乐开关 --默认开
let musicFirstLoad = true ;

let sysInfo = {}

let zIndex = 1 ;
let canTouch = true ;

let typeid = 1; //当前点击的主题类型

let innerAudioContext;

let coinNum = 0 ;







module.exports = {
    musicOn,
    musicFirstLoad,
    sysInfo,
    zIndex,
    canTouch,
    typeid,
    innerAudioContext
}